# Mascot source artwork

Full-resolution brand PNGs for the mascot poses, icon sheets, and spot
illustrations. These are the inputs to the Lottie pipeline
(`scripts/vectorize.py` + `scripts/build_lottie.py`) and the source for
any re-export of `apps/mobile/assets/`.

Kept out of the app bundle deliberately: the app ships downscaled copies.
